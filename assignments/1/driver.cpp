#include "assignment_1.h"

int main() {
	// Use this main to test the 6 functions
	string one, letter;
	int two, three;
	int numbers[];
	unsigned int size;
	CountCharacters(one, two, three);
	UpAndDown(letter);
	CountWords(one);
	ComputeMeanAverage(numbers, size);
	FindMinValue(nunmbers, size);
	FindMaxValue(numbers, size);
	return 0;
}